# NULLXES-LÆTEX Stage1 Weight Genesis + later training stack
# Do NOT install for Gate0-only work. Use after tokenizer Gate0.
#
# CRITICAL — Torch / CUDA on RunPod (RTX PRO 6000 image):
#   Image: runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404
#   Prefer IMAGE torch 2.8.0+cu128 — NEVER `pip install torch` from default PyPI.
#
#   pip install -r requirements-stage1.txt
#
#   # Restore only if image torch was overwritten:
#   pip uninstall -y torch torchvision torchaudio
#   pip install -r requirements-torch-cu128.txt \
#     --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements-stage1.txt
#
# Legacy Win11 RTX 2080: requirements-torch-cu124.txt (torch 2.4.1+cu124).
# Torch lines intentionally omitted from this file.

-r requirements-gate0.txt

# Config
omegaconf>=2.3.0
protobuf>=5.27.0,<7

# Numerical
scipy>=1.13.0

# Training utilities
rich>=13.7.0
wandb>=0.17.0

# Data pipeline
datasets>=3.0.0
pyarrow>=17.0.0

# Distributed training
accelerate>=1.0.0
deepspeed>=0.15.0

# Performance
ninja
packaging
einops>=0.8.0

# Validation / checkpoints / HF
# Pin <5 until Latex* APIs are verified on transformers 5.x
pytest>=8.0.0
safetensors>=0.4.0
transformers>=4.46.0,<5.0.0
