# H200 / CUDA 12.4 — prefer image torch; reinstall only if broken.
#   pip install -r requirements-torch-cu124.txt --index-url https://download.pytorch.org/whl/cu124
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
